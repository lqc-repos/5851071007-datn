package thesis.core.article.model.label.per_label.repository;

import thesis.core.article.model.label.per_label.PERLabel;
import thesis.utils.mongo.repo.MongoDBRepository;

public interface PERLabelRepository extends MongoDBRepository<PERLabel> {
}

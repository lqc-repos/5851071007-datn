package thesis.core.news.account.repository;

import thesis.core.news.account.Account;
import thesis.utils.mongo.repo.MongoDBRepository;

public interface AccountRepository extends MongoDBRepository<Account> {
}
